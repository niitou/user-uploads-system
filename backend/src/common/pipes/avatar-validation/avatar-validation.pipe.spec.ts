import { AvatarValidationPipe } from './avatar-validation.pipe';

describe('AvatarValidationPipe', () => {
  it('should be defined', () => {
    expect(new AvatarValidationPipe()).toBeDefined();
  });
});
